# Yei (YeiID) — 증명사진/여권사진 앱 · 프로젝트 헌법 (Always Loaded)

> 상세: `README.md` · 결제 `IAP_SETUP.md` · 스토어 `STORE_LISTING.md` · 데이터보안 `DATA_SAFETY.md`
> · 규격 점검 `MAINTENANCE.md` · 매뉴얼 `docs/manuals/` · 직원 `.codex/agents/staff-{pm,dev,qa}.toml`

## 명의 / 소속 (중요)

- **이 프로젝트의 명의 = 개인.** (법인 iTBROWN ERP 와 별개)
- **ERP 연결 금지:** 이 앱의 데이터·인증·결제·DB·git 작성자를 iTBROWN ERP 와 섞지 않는다.
  - git 작성자 이메일은 개인 이메일(`shhwang0424@gmail.com`)로 — 법인 `@itbrown.com` 사용 금지.
  - ERP Supabase/테이블/계정과 연결하지 않는다. (단, 개발실 `dev_worklog`/`dev_commands` 같은
    **개발 추적 레이어**에 진행 상황을 기록하는 것은 예외로 허용 — 그건 데이터 연결이 아니라 메모다.)

## 절대 규칙

> **코드 프리즈 해제 (2026-09-12).** 종전 1번 "앱 코드와 스토어 설정은 절대 변경하지 않는다"는
> 해제됐다. 코드 수정·릴리스 빌드 가능. 다만 결제·서명·스토어 설정에 닿는 변경은 아래 "작업 시"
> 규칙대로 먼저 본인 확인을 받는다.

1. **비밀·서명키 커밋 금지.** `android/yei-upload.keystore`, `android/keystore.properties` 는
   `.gitignore` 로 차단돼 있다. 절대 커밋·푸시하지 말 것. (분실 대비 USB·비번관리자에 별도 백업.)
2. **결제는 코드만으론 테스트 불가.** 내부 테스트 트랙 업로드 + 서명키 일치 + 플레이 설치본이어야 동작
   (sideload 불가). 상세 `IAP_SETUP.md`.

## 기술 메모

- React + TS + Vite + **Capacitor 8** (`appId: com.yei.idphoto`, appName `YeiID`).
- 결제: **RevenueCat**(`@revenuecat/purchases-capacitor`). 상품 `yei_lifetime`(일회성 ₩4,900),
  entitlement `premium`. 키는 `src/iap.ts` `RC_API_KEY`.
- 사진은 **전부 온디바이스 처리**(서버 전송 없음) — 데이터보안 신고의 핵심.
- 배경제거 모델 ~300MB 는 저장소 제외(`public/imgly/`, gitignore). 모바일 빌드는
  `npm run sync:android` 가 모델을 제외해 AAB 용량을 줄인다.

## 작업일지 (세션 규칙)

- **세션 시작 시:** 먼저 `git pull` 로 최신을 받고(충돌 시 멈추고 쉬운 말로 보고),
  `docs/worklog.md` 맨 위 항목을 읽어 "지난번 여기까지, 다음은 이것" 브리핑.
- **사용자가 "오늘 마무리"라고 하면:** `docs/worklog.md` 맨 위에 오늘 항목
  (`날짜 / 장소 / 오늘 한 일 / 다음 할 일`)을 추가하고 커밋·푸시한다.

## 작업 시

설계가 모호하거나 결제·스토어·서명·심사에 닿는 결정은 추측 금지 — 본인 확인.
직원(PM·개발자·QA)은 데이터·배포를 스스로 바꾸지 않는다: **제안 → 확인.**

### 판단·추천을 말할 때 (2026-09-20 추가)

**강한 표현을 쓰기 전에, 그 강도를 뒷받침할 근거가 있는지 먼저 확인한다.**

2026-09-20 하루에 네 번, *먼저 단언하고 본인이 되물어서야 확인하는* 일이 반복됐다.
네 번 다 정정으로 끝났고, 네 번 다 **확인이 먼저였으면 안 틀렸을 것**이다.

| 말한 것 | 실제 |
|---|---|
| "itbrown 링크가 남은 것 중 **효과가 제일 크다**" | 푸터 링크는 보일러플레이트라 가장 약한 종류 |
| "데이터 보안 신고가 **명백히 틀렸다**" | 구글도 "양식 답변은 개발자 책임"이라고만 한다 |
| "하루 4개는 **대량 생산 패턴으로 보인다**" | 근거 없는 임계값. 스팸은 하루 10~30개+ |
| "리뷰 16개니 **인앱 요청이 잘 작동한다**" | 지인 4건 포함 + 전부 구버전. 최근 유입은 0건 |

지킬 것:

1. **순위·최상급을 말하려면 비교부터 한다.** 다른 후보를 재보지 않았으면 "제일 크다"를 쓰지 않는다.
2. **임계값을 말하려면 출처를 댄다.** "N개 이상이면 위험"은 근거가 없으면 말하지 않는다.
3. **숫자로 결론 내기 전에 그 숫자의 구성을 본다.** 합계만 보고 성질을 단정하지 않는다.
4. **정책·규정 해석에 "명백히"를 쓰지 않는다.** 기관 자신의 문구를 먼저 찾아본다 —
   대개 "개발자가 판단할 사항"처럼 열려 있다.
5. **확신 수준을 구분해 말한다.**
   **확인함**(코드·콘솔·공식 문서를 직접 봄) / **추정**(근거는 있으나 확인 못 함) / **모름**.

> **시간을 더 써도 된다.** 되물린 뒤 정정하는 것보다 처음에 맞는 게 낫다.
> 본인이 검증자 역할을 하게 만들면, 본인은 매번 내 말을 의심해야 하고 그게 제일 큰 비용이다.
