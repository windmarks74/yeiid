# 작업일지 (Worklog) — Yei 증명사진 앱

> 규칙: **최신 항목이 맨 위.** 항목마다 `날짜 / 장소 / 오늘 한 일 / 다음 할 일`.
> 세션 시작 시 맨 위 항목을 먼저 읽고 브리핑한다. "오늘 마무리"라고 하면 맨 위에 오늘 항목을 추가하고 커밋·푸시.
> 명의=개인 · 코드 프리즈 해제(2026-09-12) — 코드 수정·릴리스 가능. 단 결제·서명·스토어 설정은 제안→확인.

---

## 2026-09-20 / 집 / 미국 출시 + 블로그 4편 + 마케팅 실측

**한 일**

### 1. 미국 출시 (완료 — 1.2.5 프로덕션 활성, 국가 2개)
- **미국 프리셋 분리** (`9756217`) — `us` 하나를 `us_passport` / `us_visa` 로.
  비자 기준(≤240KB)을 여권에도 적용해 화질을 깎고 있었다. 여권은 1200×1200 @600dpi 로.
- **영문 랜딩 `/en/`** (`9a2f677`) — 한국어판 번역이 아니다. 영어 로케일은 시험 규격을
  숨기므로 미국 여권·비자를 주력으로 새로 썼다. 인라인 CSS 240줄을 `landing.css` 로
  빼 두 페이지 공용. hreflang 상호 링크 + 상단 전환 버튼.
  **자동 언어 리다이렉트는 쓰지 않는다** — 구글 크롤러가 미국 IP·en-US 로 들어와
  한국어 URL이 영어로 색인되면 유일한 유입 경로를 스스로 망가뜨린다.
- **영어 스토어 자산** — 스크린샷 6장(`5e077d1`), 피처 그래픽(`a56da77`), 등록정보(`c5551b2`).
  큐넷 화면은 일부러 뺐다(영어판에 없는 기능이라 광고하면 안 된다).
- **운전면허 라벨 중립화** (`0c81719`) — 영어에서 `ID photo 35 × 45 mm`.
  미국은 주 DMV 현장 촬영이라 "Driver's license" 로 내세우면 못 쓰는 사진을 만들게 된다.
- **미국 가격은 이미 USD 2.99 였다** (`b55cdc7`) — 내가 $4.99 로 써둔 코드·랜딩을 맞췄다.
- 출시 국가에 미국 추가 → **2개국**. 1.2.5(v18) 검토 통과, 100% 게시.

### 2. 내가 만든 회귀 3건을 직접 잡았다
프리셋 분리 여파로 **같은 종류의 낡은 수치가 세 군데** 남아 있었다. 전부 고쳤다.
- 한글 스크린샷 4번 (스토어 게시본이 낡아 있었다) — 재업로드 완료
- 랜딩 미국 카드
- `/resume` 비교표
→ `check-spec-sync` 는 PNG 안 글자와 비교표를 검사하지 못한다. 생성기에 경고 주석을 달았다.

### 3. 마케팅 실측 (`f3b5220`, `63f21d3`)
- 지난 28일: 노출 1,270(+413%) → 설치 70(+126%) → 첫 실행 52. **전환율 76%**, 평점 5.0.
- **총 수익 ₩9,800, 30일 증감 0% = 결제 0건.**
- **방문→설치 76% 는 더 올릴 여지가 없다. 병목은 처음부터 끝까지 노출이다.**
- 구글 색인 **2/10**, 사유 전부 "발견됨 – 현재 색인이 생성되지 않음".
  사이트맵·내부링크(각 페이지가 나머지 7개 링크)·canonical·속도는 이미 다 돼 있다.
  **남은 변수는 외부 링크와 시간뿐** → 수동 색인 요청 반복은 시간 낭비.

### 4. 판단 3건 — 전부 "지금은 안 한다"
- **무료 횟수 5 유지** (`d7e950c`). 3으로 줄여도 예상 결제 월 0.7건이라 0건과 구분이 안 된다.
  조심스러워서가 아니라 **실험이 성립하지 않는다.** 다시 볼 조건: 월 설치 300건.
  익명 계측도 같은 이유로 되돌렸다(`6580aa4`).
- **영어 마케팅 투자 안 함.** "DS-160 240KB" 로 검색해보니 compliantpassportphoto.com,
  passsnap.app 이 **이미 같은 플레이북을 돌리고 있다.** 한국에서 통한 이유(시험 규격에
  빈틈이 있었다)가 영어권엔 없다. 미국은 유지비 0 인 옵션으로 보유.
- **Google Ads 무료 크레딧 안 씀.** 스펜드 매칭이면 공짜가 아니고, 전환 0% 라 매출도 ~0.
  UAC 저품질 설치는 이미 낮은 유지율 신호를 희석할 수 있다. 만료가 1~2월 이후면 피크에,
  아니면 그냥 보낸다 — **안 쓴 크레딧은 손해가 0이다.**

### 5. 네이버 블로그 4편 완성 (`f0a792d` `48c4a44` `45c7658` `023075f`)
1편 공무원(시즌) · 2편 큐넷(시즌) · 3편 이력서(상시) · 4편 토익(상시). 편당 이미지 3장.
각 편이 "확실히 틀린 통념" 하나씩을 반박한다 — 100KB / 디지털 금지 / 공식 규격 없음 / 115×150px.
1~3편 발행됨.

**발견 — 별개 사안으로 남김**
- 🚨 **데이터 보안 신고가 콘솔에 적용된 적이 없다** (`796c9a7`). 첫 질문이 `아니요`(수집 안 함)
  인데 `DATA_SAFETY.md` 는 처음부터 `예` 를 권장해 왔다. 애매한 영역이고(구글 ML Kit 문서도
  "개발자 책임"이라고만 한다) 경쟁 앱도 갈린다. 계측을 접었으므로 급하지 않다 → BACKLOG 4-V.

**다음 할 일**
- 4편(토익) 발행 — 3편 기준 2~3일 뒤. 이후 **주 1~2회 리듬** 유지.
- 전편 상호 링크 — **4편까지 다 올린 뒤 한 번에** (`docs/blog/README.md`).
- Google Ads 크레딧 **조건(스펜드 매칭 여부)·만료일 확인** ▸ 본인.
- `www.itbrown.com` → `yeiid.itbrown.com` 링크 ▸ 레포 밖. 외부 링크가 색인의 핵심이라 효과가 크다.
- **2026-10-20 재점검** — 네이버 검색 노출 / 구글 색인 2→? / 첫 미국 설치.
- 여권 편은 **11월** (연말 갱신 시즌). 지금 쓰면 이르다.

## 2026-09-13 / 집 / 결제 배치 2 + 규격 사전

**한 일**
- 배치 2 결제 수정 (`bd33650`)
  - 환불 후 권한 회수: entitlement 확인을 3상태(`active`/`inactive`/`unknown`)로 분리.
    예전엔 `!b.premium` 일 때만 확인해 한 번 켜진 프리미엄이 영구히 재검증되지 않았다.
    'unknown'(오프라인·오류)을 '비활성'으로 취급하면 비행기 모드의 정당 구매자가 잠기므로
    그때는 로컬 상태를 유지한다. `revokePremium` 은 `used` 를 보존한다(환불 후 무료 재획득 방지).
  - 로딩 중 게이트 fail-closed: `billing===null` 이면 다운로드 가드가 통째로 건너뛰어졌다.
- 얼굴 검출 cleanup `setFaceBusy(false)` 누락 (`64d3bf4`)
- 1.2.4 (versionCode 17) 빌드 → 내부 테스트 업로드
- **회귀 테스트: 비행기 모드 재시작 → PRO 유지 확인 (본인 폰, 1.2.4).** 핵심 위험 해소.
- **1.2.4 프로덕션 제출** (출시율 100%, 검토 중). 출시 대시보드 경고 2건은 확인 후 통과 —
  지원 기기 감소 0, 번들은 17 맞음(16 표기는 "포함되지 않는 이전 버전" 목록).
- 백로그 `4-Z` 추가: 난독화 3%→25%(기한 2027-02) · 넓은 화면 대응.
- 규격 사전 8종 배포 + 랜딩 행 전체 클릭·메뉴, "5장 무료" 문구, 네이버·구글 색인 등록

**다음 할 일**
1. **1.2.4 승인 확인** (하루 이내 예상, 승인 시 자동 게시)
2. **일주일 뒤** — `site:yeiid.itbrown.com` 로 색인 확인 + 서치콘솔/서치어드바이저 유입 검색어.
   그 검색어가 다음에 만들 규격 페이지를 정한다(추측으로 더 만들지 말 것).
3. **10월 중순** — 1.2.3 이후 설치→구매 전환 새 기준선. 지금 3.8% 는 저장 지연 버그가
   있던 1.1.9 기준이라 의미가 없다.
4. **다음 주(9/20~) — 네이버 블로그 시작.** 규격 사전 8종을 블로그 글로 옮긴다.
   인스타/페북은 안 한다(저빈도 상품 + 신규 계정 도달 0). 카페 글도 제외(홍보 금지 규정).
5. 미뤄둔 것 — 환불→회수 실검증(본인 구매 환불 필요, 로직 단순해서 생략 가능) ·
   난독화(기한 2027-02) · 스크린샷 1·2번 생성 스크립트 부재 · 무료 횟수 재검토(몇 개월 뒤)

## 2026-06-26 / 집

- **오늘 한 일:**
  - **실결제 확인 — 프로덕션 결제 정상.** 플레이 설치본에서 ₩4,900 구매·해제 동작 확인(출시 완전 검증).
  - **글로벌 §1~§6 조사 완료(코드 0) → 조건부 GO.** `docs/global-us-first-research.md`에 기록.
    - §1 시장: 미국 여권 연 2,450만~2,730만 발급·온라인 갱신(2025 출범) → 수요 분명, 단 무료앱 포화. 가격 **$4.99 1회** 권장.
    - §2 규격: 미국 프리셋 공식 일치, 단 여권/비자 섞임 2곳 → `us_passport`/`us_visa` 분리 권장. AI편집 금지(앱은 restricted로 이미 준수).
    - §3~§6: i18n UI ~150개+법무 별도 / ⚠️**개인 명의 유료앱 = 실명·주소 스토어 공개**(미국도 적용) / W-8BEN 필요·판매세는 Google 처리 / 영어 리스팅·ASO.
  - **i18n 개발 착수(슬라이스 1·2·3 완료):** 경량 영어 엔진 `src/strings.ts`(기기 로케일 자동감지, 한국어 기본) + UI 문자열 영어화(App·Paywall·usage·Cropper·ErrorBoundary·배경색·iap) + bg·imageUtils 누락분 현지화(배경 진행 **이중표시 버그 수정**) + **시험 프리셋(큐넷 등) 영어판 숨김.** tsc·vite 빌드 통과, 한국어 기본이라 라이브 영향 0.
  - 커밋 2개: `docs(global)` 조사결과 + `feat(i18n)` 코드(10파일).
- **다음 할 일:**
  - **영어 출시 전 코드:** `₩4,900`→스토어 현지화 가격(결제 영역·**본인 확인**), mm→inch, `index.html` lang/title, 영어 길이 레이아웃 점검, **슬라이스 4 법무 영문**(`legal.ts`+영어 `privacy.html` 호스팅, 초안 후 사인).
  - **스토어/운영:** 영어 리스팅·스크린샷·$4.99 설정·US 출시국가·W-8BEN.
  - **별도 확인 보류:** 개인 명의 **주소 공개 범위**(현 리스팅 점검 + 개인사업자 검토).

## 2026-06-25 / 집

- **오늘 한 일:**
  - **🚀 프로덕션 출시 완료 — 구글 플레이 라이브(검색됨).** v12(versionName 1.1.9 / versionCode 12)를 비공개 테스트 → 프로덕션으로 **승격**(새 업로드 아님 → 번호 안 올림·리빌드 없음). versionCode "업로드 vs 승격" 차이 정리.
  - **결제 코드 = 베타 그대로 확인:** v12는 결제 미변경(`src/iap.ts`가 첫 커밋 이후 무수정). "Batch 2 결제 수정"은 저장소에 실체 없음(BACKLOG상 결제·실결제테스트 완료) → 수정 없이 승격이 정답.
  - **Play 콘솔 경고 2건**(난독화 매핑 없음 / 네이티브 디버그 심볼) = **비블로킹** 판정 → 무시하고 승격. `minifyEnabled false`라 매핑 없음은 정상. BACKLOG 4-B에 v13 항목 기록.
  - **결제·코드 품질 감사(읽기만):** ① 빌링실패 = fail-closed 조회 + 로컬캐시 fail-open ② 재설치 자동복원 OK / 환불은 유료 유지(갭, 손실 미미) ③ `purchaseLifetime` `?? packages[0]` 폴백 = 미래 위험 ④ `setFaceBusy` 가드 사소 글리치. **출시 차단 없음** 확정, 개선 4건 BACKLOG v13 기록.
  - **글로벌 확장 방향 결정: 영어권(미국) 우선.** 사전 조사 체크리스트 `docs/global-us-first-research.md` 작성(시장·규격재확인·i18n·법무·가격·스토어 + 결정사항). 앱이 한국어 완전 하드코딩이라 **i18n이 핵심 신규작업.**
  - 스토어 스크린샷 변경 위치 안내(제작은 보류).
- **다음 할 일:**
  - **실결제 1회 확인(이제 플레이 설치본으로 처음 가능):** `yei_lifetime` ₩4,900 구매 → `premium` 해제 → 재설치 복원 → 본인 환불.
  - **글로벌:** `global-us-first-research.md` §1·§2(미국 시장 근거 + 규격 재확인) 조사부터 — 코드 0, go/no-go. 이후 i18n 영어는 코드변경 → versionCode↑ → 재테스트(본인 확인 후).
  - **출시 후 v12 백로그:** 큐넷 외 시험 프리셋(KPC·공무원·토익) 정정 + 빌드 품질 2건·결제 감사 4건은 다음 v13 리빌드에 묶기.
  - 스토어 스크린샷 교체(원하면 폰 캡처 → 규격 맞춤).

## 2026-06-19 / 집

- **오늘 한 일:**
  - **개인정보처리방침 정리:** 옛 파일 `privacy-policy-yei.html`(6/3, ML Kit 누락)이 워킹트리에서 삭제돼 있던 것 발견 → git 복구. **`privacy.html`(6/12 신본, ML Kit 포함)이 정식본**임을 `legal.ts`·`BACKLOG`로 확정. GitHub Pages에서 두 URL 모두 라이브 확인(미푸시라 라이브 영향 없었음).
  - **v11 빌드·설치:** 폰의 v10(6/6 빌드)이 옛 개인정보처리방침 문구라, 새 문구 반영 위해 **v11(versionName 1.1.8 / versionCode 11)** 빌드. 서명 충돌(Play 재서명본)로 기존 삭제 후 **폰(SM-F966N) 사이드로드 설치·실행 확인**(결제는 사이드로드라 미동작 = 정상). 서명 **AAB 생성·보관**: `aab_archive/Yei-v11-1.1.8.aab`.
  - **§7 규격 라이브 재확인:** 큐넷·토익·KPC·공무원 공식 대조. **큐넷(유일 LIVE) 공식 일치 → 출시 안전.** 나머지는 `isSelectable` 게이팅으로 숨김 유지(무위험). 정정값(KPC px범위·공무원 350KB·토익 6MB 등)과 v12 활성화 TODO를 `MAINTENANCE.md`에 기록.
- **다음 할 일:**
  - **내일(6/20, 베타 종료) Play 콘솔:** `Yei-v11-1.1.8.aab` 업로드 → 개인정보처리방침 URL=`.../privacy.html` 확인 → (내부테스트 결제 1회 확인) → **프로덕션 승격·심사 제출.**
  - **출시 후 v12:** 공무원(용량 350KB)·토익(표기 6MB)·KPC(px 공식 범위) 값 정정 후 `verified=true`로 시험 프리셋 확장.

## 2026-06-13 / 집

- **오늘 한 일:** 프로젝트 등록 — 로컬 `photo_App` 을 원격 `yeiid.git` 에 첫 백업(서명키 등 비밀은 .gitignore 제외),
  작업일지 규칙 + 직원 3명(PM·개발자·QA) 에이전트 설치, 명의=개인/ERP 연결 금지 명시(CLAUDE.md·README, git 작성자 개인 이메일).
  경험 추출 매뉴얼 2개(결제연동·플레이콘솔출시) **완성**(인터뷰 반영: 결제 완료/콘솔 등록 완료/경고 없음, 가장 힘들었던 건 콘솔·RC 메뉴 위치 찾기 → "메뉴 위치 빠른 찾기" 정리). ERP 지식문서로도 복사.
- **다음 할 일:** 출시 전 규격 정확성 점검(MAINTENANCE 미해결: 토익/KPC/큐넷/공무원 px·용량) → 프로덕션 제출. 목표 출시 6월 말. 6/18 출시 신청 준비 리마인드.
